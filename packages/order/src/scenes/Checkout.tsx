import type { FC } from "react";

import { Button } from "@radix-ui/themes";

const Checkout: FC = () => {
  return (
    <div>
      Checkout Page <Button>Checkout</Button>
    </div>
  );
};

export default Checkout;
