import type { FC } from "react";

import { Button } from "@radix-ui/themes";

const Shipping: FC = () => {
  return (
    <div>
      Shipping Page <Button>Shipping</Button>
    </div>
  );
};

export default Shipping;
