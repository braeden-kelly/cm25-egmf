import type { FC } from "react";

import { Button } from "@radix-ui/themes";

const Cart: FC = () => {
  return (
    <div>
      Cart Page <Button>Cart</Button>
    </div>
  );
};

export default Cart;
