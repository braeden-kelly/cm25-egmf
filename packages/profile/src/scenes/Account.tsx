import type { FC } from "react";

import { Button } from "@radix-ui/themes";

const Account: FC = () => {
  return (
    <div>
      Account Page <Button>Account</Button>
    </div>
  );
};

export default Account;
